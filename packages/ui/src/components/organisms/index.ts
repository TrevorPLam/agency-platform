/**
 * Organisms — compositions of molecules (and atoms) forming distinct UI sections.
 * Add interaction states (loading/empty/error) and clear data contracts here.
 * App-specific organisms (e.g. SiteHeader, ContactForm) live in apps; shared ones go here when repeated.
 */
// No shared organisms yet. Add when a pattern repeats across multiple apps.
