# Riley Day Care — Build-Out Spec

**Source:** The full spec (content, structure, and requirements) is at: https://share.google/3MQFHVDZjzpTBa8kV (auth/share required).

**Purpose of this file:** Paste the key sections from that spec here (pages, features, copy, forms, blog requirements) so implementation can be done against a single source of truth. Until then, the app at `apps/clients/riley-day-care/` is the converted riverside-hotel scaffold with Riley Day Care branding and placeholder content.

---

## Implementation checklist (execute when spec content is added above)

- [ ] **Content and copy** — Align all pages (home, about, programs, contact, blog) with spec: headlines, body copy, CTAs, meta descriptions.
- [ ] **Pages** — Add any pages required by the spec (e.g. enrollment steps, FAQs, staff, gallery). Remove or repurpose pages not in spec (e.g. `dark-mode-test`).
- [ ] **Forms** — Contact/enrollment: ensure fields, validation, and submit behavior match spec (Server Action, Supabase, or email).
- [ ] **Blog** — If spec defines blog structure (categories, featured posts, layout), implement or adjust `apps/clients/riley-day-care/src/app/blog/` and `[slug]` accordingly.
- [ ] **Design tokens** — Adjust `packages/design-tokens/tokens/clients/riley-day-care.json` if the spec specifies brand colors/typography.
- [ ] **Final pass** — Navigation, footer links, and any spec UX (e.g. tour scheduling, enrollment flow) implemented and tested.

---

*Add spec content below this line when available.*
